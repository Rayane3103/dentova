import { EmptyAdminState } from "@/components/admin/EmptyAdminState";

export default function NewsletterPage() {
  return (
    <EmptyAdminState
      description="Newsletter subscribers will appear here after the API route writes to MongoDB."
      title="Newsletter"
    />
  );
}
